<%= bootstrap_form_for ([@company, @employee])  do |f| %>
    <%= f.text_field :first_name %>

    <%= f.text_area :last_name %>


    <%= f.submit 'Update' %>

<% end %>
<br>
<%= link_to 'Destroy', company_employee_path,
            method: :delete,
            data: {
                confirm: 'Are you sure?'
            }
%><br>
<%= link_to 'Back', company_employees_url(@company) %>
