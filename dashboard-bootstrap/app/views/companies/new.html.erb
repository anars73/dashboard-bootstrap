<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

    <%= f.submit 'Create' %>
    <%= link_to 'Back', companies_path %>
<% end %>