<%= bootstrap_form_for ([@company, @employee])  do |f| %>
    <%= f.text_field :first_name %>

    <%= f.text_field :last_name %>

    <%= f.submit 'Create' %>
<% end %>
<br>

<%= link_to 'Back', company_employees_url %>